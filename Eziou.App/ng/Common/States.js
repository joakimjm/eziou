(function () {
    'use strict';

    var id = 'App';

    angular.module(id)
        .config(["$stateProvider", function ($stateProvider) {
            /*
             * Redirect: Set the default app url
             */
            //$routeProvider.otherwise('/');
        }]);
})();